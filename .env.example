# Meta Ad Library API token (optional — leave blank to use mock data)
META_API_TOKEN=

# Google Gemini API key (optional — needed for AI-powered analysis)
GEMINI_API_KEY=

# Set to True to use local mock data instead of live API calls
USE_MOCK_DATA=True
